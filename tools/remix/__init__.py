"""octabam's remix engine: modules, the index, and the builds that compose them.

`modules/<name>/manifest.py` declares a contribution; `tools/remix/schema.py`
defines what it may declare; `tools/remix/registry.py` finds them all.
"""
