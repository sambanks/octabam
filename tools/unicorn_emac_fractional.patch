--- a/src/qemu/target/m68k/helper.c	2026-09-08 16:33:21.796598024 +1000
+++ b/src/qemu/target/m68k/helper.c	2026-09-08 16:33:22.130278381 +1000
@@ -663,10 +663,17 @@
 
 uint64_t HELPER(macmulf)(CPUM68KState *env, uint32_t op1, uint32_t op2)
 {
-    uint64_t product;
+    /* octabam: ColdFire fractional mode is SIGNED 1.31 x 1.31. The 2.62
+       product is shifted left one bit (the redundant sign bit) to 1.63 and
+       its upper 40 bits are accumulated, so MOVCLR (ACC[39:8]) returns
+       product >> 31, not >> 32 as the unsigned form below computed. The
+       Octatrack firmware's reciprocal tables (2^31 / N) and its tempo
+       converter only reproduce hardware with this alignment. */
+    int64_t product;
     uint32_t remainder;
 
-    product = (uint64_t)op1 * op2;
+    product = (int64_t)(int32_t)op1 * (int64_t)(int32_t)op2;
+    product <<= 1;
     if (env->macsr & MACSR_RT) {
         remainder = product & 0xffffff;
         product >>= 24;
@@ -677,7 +684,7 @@
     } else {
         product >>= 24;
     }
-    return product;
+    return (uint64_t)product;
 }
 
 void HELPER(macsats)(CPUM68KState *env, uint32_t acc)
@@ -883,14 +890,14 @@
     } else if (env->macsr & MACSR_RT) {
         /* 32-bit rounding.  */
         rem = val & 0xff;
-        val >>= 8;
+        val = (uint64_t)((int64_t)val >> 8);    /* octabam: the accumulator is SIGNED */
         if (rem > 0x80)
             val++;
         else if (rem == 0x80)
             val += (val & 1);
     } else {
         /* No rounding.  */
-        val >>= 8;
+        val = (uint64_t)((int64_t)val >> 8);    /* octabam: arithmetic, not logical */
     }
     if (env->macsr & MACSR_OMC) {
         /* Saturate.  */
@@ -901,8 +908,14 @@
                 result = val & 0xffff;
             }
         } else {
-            if (val != (uint32_t)val) {
-                result = ((uint32_t)(val >> 63) & 0x7fffffff);
+            /* octabam: with a signed product (macmulf above) a negative
+               accumulator reaches here sign-extended; the original logical
+               shift made every negative value look like an overflow and the
+               original saturation value for that case was 0, so a saturating
+               MAC (MACSR 0xa0, the recorder's fade stage) zeroed every
+               negative sample. Compare as signed, saturate to the sign. */
+            if ((int64_t)val != (int64_t)(int32_t)val) {
+                result = ((int64_t)val < 0) ? 0x80000000u : 0x7fffffffu;
             } else {
                 result = (uint32_t)val;
             }
--- a/src/qemu/target/m68k/translate.c	2026-09-08 16:33:21.798327962 +1000
+++ b/src/qemu/target/m68k/translate.c	2026-09-08 16:33:22.073642489 +1000
@@ -5794,7 +5794,13 @@
     }
 #endif
 
-    if (insn & 0x100)
+    /* octabam: MAC vs MSAC is bit 8 of the EXTENSION word on ColdFire
+       (CFPRM MAC/MSAC encodings; binutils; every msac in the Octatrack
+       image carries ext 0x01xx with opcode bit 8 clear), not bit 8 of the
+       opcode, which the original tested. With the opcode bit the
+       sequencer's frame builder ADDED where it subtracts, so every trig's
+       sub-frame offset came out 0. */
+    if (ext & 0x100)
         tcg_gen_sub_i64(tcg_ctx, MACREG(acc), MACREG(acc), s->mactmp);
     else
         tcg_gen_add_i64(tcg_ctx, MACREG(acc), MACREG(acc), s->mactmp);
